PORT=4000
# Vacío = proxy de package.json hacia el backend (recomendado en desarrollo)
REACT_APP_API_URL=
# Si el proxy no funciona en tu entorno, descomenta:
# REACT_APP_API_URL=http://localhost:9090
